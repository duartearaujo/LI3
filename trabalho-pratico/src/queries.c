#include <stdio.h>
#include <stdlib.h>
#include "../include/queries.h"


void dealwithquerie (char **argv) {
    int q = atoi (argv[0]);
    switch (q)
    {
    case 1:
        break;
    
    default:
        break;
    }
    return;
}